const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { await prisma.systemLog.create({ data: { jobName: 'fetch-news', status: 'SUCCESS', details: 'Verified: Total 11 new articles (NewsAPI: 4, CNA: 7) via Gemini [gemini-3.5-flash-lite]. No relevant threats detected.' } }); console.log('Mock log created!'); } main().catch(console.error).finally(() => prisma.$disconnect());
